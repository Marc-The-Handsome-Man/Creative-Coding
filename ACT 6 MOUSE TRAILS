function setup (){
  createCanvas(windowWidth,windowHeight);
  noStroke();
  background(0)
  
}

function draw(){
  fill(random(150), 0, random(255,255), 150);
  let size = random(5,20);
  rect(mouseX + random(-20,20), mouseY + random(-20,20),size);  
}
